import React from 'react';
import styled from 'styled-components';

const MainContainer = styled.main`
  display: grid;
`;
const Main = () => {
  return <MainContainer>This is the main</MainContainer>;
};

export default Main;
